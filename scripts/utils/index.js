"use strict";

let parsePath = require("./parse-path"),
    createNewFile = require("./create-new-file"),
    gulpHandleErrors = require("./gulp-handle-errors");

module.exports = {
    parsePath,
    createNewFile,
    gulpHandleErrors
};
